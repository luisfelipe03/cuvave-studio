/**
 * Perfis de dispositivo (Device Profiles) da família Cuvave/M-VAVE — um
 * profile declarativo por pedal (id, memoryMap, parameters, irFormat,
 * buildSysEx). O primeiro profile real (cube-baby) entra no M2, depois do
 * protocolo validado no M1 (ver cuvave-spec.md § "Perfis de dispositivo").
 * Por enquanto isto é só o placeholder que prova que o pacote é importável
 * de dentro de apps/web via npm workspaces.
 */
export const PROFILES_PACKAGE_READY = true
